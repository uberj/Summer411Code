#include <stdio.h>
#include <stdlib.h>
#include <math.h>

int main (int argc, char *argv[]){
	int num, result;
	num = 9;
	result = sqrt(num);
	printf("the sq root for %d is %d", num, result);
	return 0;
}
